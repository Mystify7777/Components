{
	"name": "components",
	"version": "0.1.0",
	"private": true,
	"description": "Reusable UI component system monorepo",
	"packageManager": "pnpm@9.12.3",
	"scripts": {
		"dev": "turbo run dev --parallel",
		"build": "turbo run build",
		"lint": "turbo run lint",
		"test": "turbo run test",
		"typecheck": "turbo run typecheck",
		"format": "turbo run format"
	},
	"devDependencies": {
		"turbo": "^2.1.3"
	}
}
